Install notes (work-in-progress)
--------------------------------

- Python-dev
- Python-setuptools
- PIP

- Reids:
  
   $ sudo add-apt-repository ppa:rwky/redis
   $ sudo apt-get update
   $ sudo apt-get install redis-server
